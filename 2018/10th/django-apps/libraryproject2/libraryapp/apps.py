from django.apps import AppConfig

#These are the settings for the library app, mainly the Configuration to be added to the INSTALLED_APPS section in settings.py
class LibraryappConfig(AppConfig):
    name = 'libraryapp'
